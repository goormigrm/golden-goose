/* ============================================================
 * 배포 전에 개발자가 채워 두는 값.
 * 여기만 채워두면 스트리머는 [치지직 로그인] → [동의] 두 번만 누르면 된다.
 * ============================================================ */

/** 치지직 개발자센터 Client ID (공개돼도 되는 값) */
const PRESET_CLIENT_ID = 'afd9f0b7-64f7-4cf9-baa0-ea25d0a3354b';

/** CORS 프록시(Cloudflare Worker) 주소.
 *  openapi.chzzk.naver.com 은 브라우저에서 직접 호출이 막혀 있어 REST는 이걸 거친다.
 *  Client Secret 은 이 코드가 아니라 워커의 환경변수(CHZZK_CLIENT_SECRET)에만 존재한다.
 *  소스: proxy/worker.js — `cd proxy && npx wrangler deploy` 로 재배포. */
const PRESET_PROXY_URL = 'https://golden-goose-proxy.1117tkdrms.workers.dev';

/* ---------------- 게임 규칙 ---------------- */

/** 후원 1,000 치즈당 쓰다듬기 횟수 (치지직 후원 단위는 '원'이 아니라 '치즈') */
const CLICKS_PER_1000 = 10;

/** 후원 인정 단위(치즈). 이 단위로 내림해서 환산한다. 1,500 치즈 → 1,000 치즈분(10회) */
const DONATION_UNIT = 1000;

/** 후원으로 대신 쓰다듬는 속도(초당 횟수).
 *  **금액과 무관하게 항상 같다.** 그래서 큰 후원일수록 그만큼 오래 화면에 남고,
 *  후원자는 자기 후원으로 어떤 알이 나오는지 끝까지 볼 수 있다.
 *    1,000 치즈(10회)  →  0.7초   ※ 아래 최소 노출 시간에 걸려 실제로는 2.6초 노출
 *    10,000 치즈(100회) →  6.7초
 *    50,000 치즈(500회) → 33초
 *   100,000 치즈(1,000회) → 67초
 *  올리면 전체가 빨라지고 노출도 짧아진다. 내리면 반대. */
const DONATION_CLICKS_PER_SEC = 15;

/** 거위가 스스로 알을 낳는 주기(ms) */
const DROP_INTERVAL = 3 * 60 * 1000;

/** 쓰다듬기 1회당 즉석에서 알을 낳을 확률.
 *  밸런스 기준 — 사람이 유지 가능한 속도를 초당 5.5회로 잡고,
 *  도감 34종 완성이 보통 30분 / 대부분 1시간 안에 끝나도록 맞춘 값이다.
 *    0.06 × 5.5회/초 + 3분 타이머 = 분당 약 20개
 *    완성 시간: 25% 18분 · 중앙값 26분 · 75% 37분 · 90% 50분 · 99% 84분
 *  이 값이나 eggs.js 의 등급 weight 를 건드리면 위 수치가 통째로 흔들린다.
 *  (HANDOVER.md "밸런스" 절에 계산식이 있다) */
const CLICK_DROP_CHANCE = 0.06;

/** 영상 도네도 인정할지 */
const ACCEPT_VIDEO_DONATION = true;
