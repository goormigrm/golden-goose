# HANDOVER — GOLDEN GOOSE 24K

> 다음 세션(사람/AI 누구든)이 이 문서만 읽고 이어서 작업할 수 있도록 남기는 인수인계 문서.
> 마지막 업데이트: **2026-09-01** · 상태: **개발·배포 완료, 치지직 연동 실동작 확인 완료** (방송 실전 투입 전)

## 1. 한눈에 보기

| 항목 | 값 |
|---|---|
| 무엇 | 순금 24K 황금알을 낳는 거위 — 클릭 카운터 + 랜덤 등급 드롭 + 도감 수집형 클리커 |
| 라이브 | https://goormigrm.github.io/golden-goose/ |
| 저장소 | https://github.com/goormigrm/golden-goose (main push → Pages 자동 반영, 빌드 없음) |
| 스택 | **빌드 없음.** 정적 HTML/CSS/바닐라 JS + socket.io-client **2.5.0 고정**(vendor 폴더) |
| 치지직 앱 | **이 게임 전용 앱** · Client ID `afd9f0b7-64f7-4cf9-baa0-ea25d0a3354b` |
| 프록시 | https://golden-goose-proxy.1117tkdrms.workers.dev (Cloudflare, 계정 1117tkdrms) · 소스 `proxy/worker.js` |
| 관련 저장소 | [mukbang-roulette](https://github.com/goormigrm/mukbang-roulette) — 치지직 연동 코드의 출처 (앱·워커는 서로 완전히 별개) |
| 저작권 | © 2026 goormigrm |

**검증 상태**: 로컬 서버에서 클릭·정기 산란·즉석 산란·도감·후원 환산(테스트 후원)·저장/복원 전부 동작 확인.
**배포 검증(2026-09-01)**: 정적 파일 7개 전부 200, 콘솔 에러 없음, `io`/`Chzzk`/`__gg` 로드 확인,
사이트가 계산한 리디렉션 URI = `https://goormigrm.github.io/golden-goose/`,
프록시 워커가 이 오리진을 CORS 허용함(`OPTIONS /open/v1/sessions/auth` → `Access-Control-Allow-Origin` 확인).

**연동 검증(2026-09-01, 사용자 직접)**: 개발자센터 리디렉션 URI 등록 → [치지직 로그인] → 동의 →
배지 🟢 후원 수신 중까지 확인. 치지직 테스트 후원 15건이 실제로 수신되어 밴드 칩·배너·쓰다듬기 환산까지 정상 동작.

**아직 안 한 것**: 실제 방송에서의 시청자 후원 수신, OBS 캡처 리허설.

## 2. 확정된 규칙

- **쓰다듬기**: 거위 클릭 1회 = 카운터 +1. 상한 없음.
- **즉석 산란**: 쓰다듬기 1회마다 0.4%(`CLICK_DROP_CHANCE`) 확률로 알 1개.
- **정기 산란**: 3분(`DROP_INTERVAL`)마다 1개. 창을 닫아둔 시간도 누적되어 복귀 시 한꺼번에 지급(**상한 없음** — 임의 제약 금지 원칙).
- **등급 확률**: 일반 60 / 고급 25 / 희귀 10 / 영웅 4 / 전설 0.9 / 신화 0.1 (%). 등급을 먼저 뽑고, 같은 등급 안에서는 균등.
- **알 34종**. 그중 **6종이 순금 24K(순도 999.9)** — 금박 0.05g / 24K황금알 18.75g / 골드바 1kg / 태양 8kg / 창세 100kg / 거위신 1t. 보유 순금 총량을 g→kg→t로 환산 표시.
- **후원 환산**: 1,000원 = 쓰다듬기 10회. 1,000원 단위 내림. 상한 없음. 영상 후원 인정(`ACCEPT_VIDEO_DONATION`).
  환산된 자동 쓰다듬기는 **6초에 걸쳐 소진**되고, 그 클릭에도 즉석 산란 확률이 그대로 적용된다.
- **저장**: localStorage `gg:save-v1`. 0.8초 디바운스 + 15초 주기 + 탭 숨김/종료 시 즉시 저장.

### 화면 원칙 (사용자 지시로 확정 — 되돌리지 말 것)

1. **획득이 클릭을 막지 않는다.** 알을 얻어도 모달을 띄우지 않는다. 우하단 알림 카드(`.drop-stack`,
   `pointer-events:none`)가 뜨고 등급에 따라 3.6~7.6초 뒤 스스로 사라진다. 영웅 이상은 화면 플래시가 추가된다.
   모달(`#reveal`)은 **둥지·도감의 알을 사용자가 직접 눌렀을 때만** 연다.
   → 이유: 클리커에서 알림을 닫으러 마우스를 옮기는 동작 자체가 스트레스.
2. **PC 우선 3단 레이아웃, 탭 없음.** 왼쪽 기록·확률 / 가운데 거위 / 오른쪽 둥지·도감·24K 목록.
   1240px 이하 2단, 820px 이하 1단. 카드 테두리로 가두지 않고 얇은 구분선만 쓴다.
3. **후원 밴드를 최상단에 상시 노출** (`.donband`, 상단바 아래 sticky). 이 게임의 중심이라 탭에 숨기지 않는다.
4. **화면에 노출하지 않는 것**
   - 즉석 산란 확률 수치 (등급 확률은 공개)
   - 후원 **누적 금액**, 후원 **누적 쓰다듬기 횟수** — 수입이 추정되므로. 누적 **건수**만 공개.
     (`state.don.sum`/`clicks`는 내부 기록으로 남지만 렌더하지 않는다)
   - 후원 밴드 칩에도 금액을 넣지 않는다. 금액은 도착 순간의 배너에만 잠깐 뜬다.
   - 연동/개발자·테스트용 정보 일체 — 리디렉션 URI 안내, 데모/테스트 후원 버튼 등.
     테스트는 콘솔 `__gg.donate(원, 닉, 메시지)` 로만 한다.
5. **카피라이트는 하단 중앙 정렬.**
6. **후원 밴드는 두 줄이다.** 윗줄 = 규칙 + **지금 처리 중인 후원 한 건** + `+N 대기`,
   아랫줄 = **누적 내역**(누적 건수 + 최근 후원 칩이 왼쪽부터 쌓임).
   화면 위에 떠다니는 배너 오버레이는 없앴다 — 카운터를 가리던 데다 자리가 겹쳤다.
7. **표시와 숫자가 같은 후원을 가리켜야 한다.** 후원의 표시 큐와 처리 큐를 하나로 합쳤다
   (`donQueue` / `donActive`). "지금" 칸에 뜬 그 건이 떠 있는 동안 **딱 그 건의 쓰다듬기만**
   `dur` 시간에 걸쳐 나눠 소진하고, 진행 막대와 `쓰다듬기 3 / 10회` 가 같이 올라간다.
   예전에는 쓰다듬기는 도착 즉시 전부 소진되는데 내역만 큐에 밀려 늦게 떠서 어긋났다.
   밀린 건수만큼 표시 시간을 `DON_SHOW`(2.6초) → `DON_SHOW_MIN`(1.1초)까지 줄이되 한 건도 건너뛰지 않는다.
8. **PC(1241px 이상)에서는 페이지 스크롤이 없다.** `body{height:100vh;overflow:hidden}`,
   상단바·후원 밴드는 `position:static`, 각 칸(`.col`)이 자기 안에서만 스크롤한다.
   오른쪽 칸은 flex로 둥지·도감이 높이를 나눠 가져 칸 안에 또 스크롤이 생기지 않는다.
   거위 크기는 `min(384px,78vw,36vh)`로 화면 높이에 맞춰 줄어든다(세로 800px에서도 타이머가 안 잘림).
   ※ 레이아웃 미디어쿼리가 기본 규칙보다 **앞에** 있으므로, 기본값을 덮으려면 `.mid-inner .goose-btn`
   처럼 선택자를 한 단계 올려야 한다. (같은 특이도면 뒤에 오는 기본 규칙이 이긴다 — 실제로 한 번 겪음)
9. **알마다 공로자 이름표.** `owned[id].by`(처음 뽑은 사람) / `lastBy`(마지막). 값은
   `{k:'don',n:'닉'} | {k:'hand'} | {k:'goose'}`. 후원 대기열을 후원자별로 묶어(`autoQueue=[{nick,left}]`)
   그 몫의 쓰다듬기에서 나온 알에 정확히 그 사람을 기록한다. 둥지 셀·획득 카드·상세 창에 표시.
   예전 세이브에는 `by`가 없어 이름표가 안 뜨지만, 그 알을 한 번 더 뽑으면 채워진다.
10. **알이 나오면 가운데 거위에서 실제로 알이 나온다.** 꽁무니에서 톡 튀어나와 바닥에 튕기고
   둥지 쪽으로 날아가며 사라진다(`.fx-egg`). 여러 개를 한꺼번에 낳으면 **높은 등급부터** 최대
   `EGG_FX_MAX`(8)개만 그린다 — 화면이 알로 뒤덮이지 않게 하기 위한 그리기 한도일 뿐,
   실제 획득 개수에는 제한이 없다.

## 2-1. 밸런스 (사용자 지시로 확정)

**목표: 도감 34종 완성이 보통 30분, 최대 1시간.** 사람이 유지 가능한 속도를 **초당 5.5회**로 가정.

| 값 | 위치 |
|---|---|
| `CLICK_DROP_CHANCE = 0.06` | config.js |
| 등급 weight 45 / 26 / 17 / 8 / 3 / 1 (%) | eggs.js `TIERS` |
| `DROP_INTERVAL = 3분` | config.js |

결과: 알 **분당 약 20개**, 30분간 쓰다듬기 약 9,900회,
완성 시간 **25% 18분 · 중앙값 26분 · 75% 37분 · 90% 50분 · 99% 84분**.

완주 시간을 지배하는 건 **신화 3종(종당 0.33%)** 이다. 등급 weight나 산란 확률을 바꾸면
아래 스크립트로 반드시 다시 재보고 커밋할 것.

```python
import random
CPS=5.5; TIMER=180.0; CH=0.06                     # 초당 클릭 / 타이머(초) / 산란 확률
COUNTS={'common':7,'uncommon':7,'rare':7,'epic':6,'legendary':4,'mythic':3}
W={'common':45,'uncommon':26,'rare':17,'epic':8,'legendary':3,'mythic':1}
pool=[(t+str(i),(w/100.0)/COUNTS[t]) for t,w in W.items() for i in range(COUNTS[t])]
names=[x[0] for x in pool]; probs=[x[1] for x in pool]; rate=CPS*CH+1/TIMER
out=[]
for _ in range(6000):
    seen=set(); n=0
    while len(seen)<len(names):
        seen.add(random.choices(names,probs)[0]); n+=1
    out.append(n/rate/60)
out.sort(); q=lambda f: out[int(len(out)*f)]
print("p25=%.0f median=%.0f p75=%.0f p90=%.0f p99=%.0f (min)"%(q(.25),q(.5),q(.75),q(.9),q(.99)))
```

※ 오프라인 누적은 3분당 1개라 하룻밤(8시간) 자리를 비우면 160개가 쌓인다. 완주 목표에는 영향이 작다.

## 3. 아키텍처

```
[GitHub Pages 정적 파일] ──REST──> [Cloudflare Worker 프록시] ──> openapi.chzzk.naver.com
        └──────웹소켓(Socket.IO 2.x, 후원 실시간)──직접──> 치지직 세션 서버
```

- openapi.chzzk.naver.com은 **브라우저 CORS 차단** → REST(토큰/세션/구독)만 워커 경유. 웹소켓은 직접.
- Client Secret은 **워커 Secret 환경변수에만** 존재. 이 저장소·프론트에 없음.
- 치지직 세션 서버는 **Socket.IO 2.0.3까지만** 지원 → `vendor/socket.io-2.5.0.slim.js` 고정. v4 클라이언트는 붙지 않는다.
- 빌드 도구가 없어 `index.html`을 그대로 서빙하면 끝. Pages는 `Deploy from a branch` (main / root).

### 파일

| 파일 | 역할 |
|---|---|
| `index.html` | 화면 구조, 거위 SVG, 24K 인증 각인 SVG |
| `styles.css` | 다크+금색 테마 전체 |
| `config.js` | **규칙 상수 + Client ID + 프록시 주소** — 값 조정은 여기만 |
| `eggs.js` | 등급표 `TIERS`, 알 34종 `EGGS`, 알 SVG 렌더러 `eggSvg()` |
| `chzzk.js` | OAuth → 세션 → 후원 구독 + 지수 백오프 재연결 (`rr/src/chzzk.ts`의 바닐라 포팅) |
| `game.js` | 상태/저장, 클릭, 산란, 후원 환산 큐, 렌더, 연출 |
| `vendor/` | socket.io-client 2.5.0 slim (mukbang-roulette의 node_modules에서 복사) |
| `proxy/` | 전용 Cloudflare Worker (`golden-goose-proxy`) + wrangler 설정 |

## 4. 체인지로그

1. (초기) 게임 코어 — 거위 SVG, 클릭 카운터, 3분 정기 산란 + 0.4% 즉석 산란, 알 34종/6등급, 도감·둥지·기록·확률 탭, localStorage 저장, 오프라인 누적 지급, WebAudio 효과음
2. **24K 순금 강조**(사용자 요청) — 헤더 24K 칩, 회전하는 순금 인증 각인(999.9), 24K 스트립(보유 순금 실시간), 알마다 재질 표기, 순금 6종에 함유량 부여 + 확률 탭에 24K 목록, 알 카드 24K 태그
3. **치지직 후원 연동**(사용자 요청) — 1,000원 = 쓰다듬기 10회. `chzzk.js` 포팅, 후원 탭(내역/합계/테스트 후원/리디렉션 URI 안내), 후원 배너 연출, 자동 쓰다듬기 큐(6초 소진), 초당 쓰다듬기(cps)에 자동분 반영
4. 문서 — README / `docs/공지글-모음.md` / 이 문서
5. **GitHub Pages 배포**(저장소 `golden-goose`) — 파일 7개 200, 콘솔 에러 없음 확인
6. **전용 치지직 앱·프록시로 분리**(사용자 요청) — 새 Client ID 발급, `proxy/`에 전용 워커
   `golden-goose-proxy` 배포 + Secret 등록, `config.js` 교체. 룰렛과 완전히 독립
7. **카피라이트 정리**(사용자 요청) — 푸터를 `© 2026 goormigrm · GOLDEN GOOSE 24K`로 교체,
   외부 게임 언급 제거, README에 저작권 절 추가
8. `fix` CSS/JS에 버전 쿼리(`?v=`) — 빌드 해시가 없어 재방문 브라우저가 옛 `config.js`를 쓰던 문제.
   **파일을 고칠 때마다 `index.html`의 `?v=` 숫자를 올릴 것**
9. **UI 전면 개편**(사용자 요청) — 탭 제거 후 PC 우선 3단 레이아웃, 후원 밴드 최상단 상시 노출,
   획득 모달을 논블로킹 알림 카드로 교체, 즉석 산란 확률·후원 누적 금액/쓰다듬기 횟수 비노출,
   화면에서 개발자용 정보 제거, 카피라이트 하단 중앙 정렬.
   부수 수정: `html,body{height:100%}`가 sticky/스크롤과 충돌해 제거, 렌더를 프레임당 1회로 합침
10. **난이도 재조정**(사용자 요청) — "완결 보통 30분·최대 1시간, 초당 5~6회 클릭 기준".
    산란 확률 0.4%→6%, 등급 weight 60/25/10/4/0.9/0.1 → 45/26/17/8/3/1.
    이전 설정은 완성 중앙값이 **47시간**이라 사실상 완주 불가였다. 지금은 중앙값 26분.
    알림 카드 유지 시간도 분당 20개 속도에 맞춰 단축
11. **데모 후원 버튼**(사용자 요청, 게임 소개용) — 상단바 🎬 → 랜덤 1건 / 연속 5건 / 러시 15건 /
    고액 1건 / 금액 직접. 닉네임 30종·메시지 25종·금액표에서 매번 새로 뽑아 같은 화면이 두 번 안 나온다.
    실제 후원과 같은 `handleDonation()` 경로. `?demo=0`으로 숨김
12. **산란 연출**(사용자 요청) — 알을 얻을 때 가운데 거위 꽁무니에서 실제로 알이 나와
    바닥에 튕긴 뒤 둥지 쪽으로 날아간다. 영웅 이상은 등급색 링이 퍼진다
13. **후원 밴드 확대 + 배너 전건 표시**(사용자 요청) — 밴드 높이·글자·칩을 키우고,
    `외 N명` 합치기를 없애고 큐로 바꿔 **모든 후원을 한 건씩 전부** 띄운다(밀리면 표시 시간 단축)
14. **PC 무스크롤 레이아웃**(사용자 요청: "밴드에 기록·둥지가 가려서 불편") — 페이지 스크롤을 없애고
    각 칸이 자기 안에서만 스크롤하도록. 24K 목록을 왼쪽으로 옮겨 오른쪽은 둥지·도감이 높이를 나눔.
    거위 크기를 vh 기준으로 바꿔 세로가 짧은 화면에서도 타이머가 안 잘림
15. **알 이름표**(사용자 요청) — "누구 후원으로 뽑았는지 크게 표시해서 후원자가 뿌듯하도록".
    후원 대기열을 후원자별로 묶고 알마다 공로자를 기록해 둥지·획득 카드·상세 창에 표시
16. **등급별 수집 막대 수정**(사용자 지적: "수집해도 막대가 안 움직인다") — 원인이 두 개였다.
    - (a) 계산식: "보유 개수 ÷ 전체 개수"라 일반이 45%를 차지해 높은 등급 막대가 0%에 붙어 있었다
      → "모은 종류 ÷ 그 등급 종류 수"로 변경. 라벨도 "N개"에서 "1 / 3"으로.
    - (b) **진짜 버그**: `.fill` 이 `<span>`(인라인)인데 부모 `.track` 이 flex 가 아니어서
      width/height 가 전혀 먹지 않았다. **처음부터 한 번도 그려진 적이 없다.**
      → `.track` / `.fill` 에 `display:block` 명시. 타이머(`.timer-fill`)·도감(`.dex-fill`) 막대는
      `<div>` 라서 멀쩡했다. ※ 앞으로 막대를 추가할 때 `<span>` 을 쓰면 반드시 display 를 지정할 것.

    다 모으면 막대가 빛나고(`.tier-row.done`), 보유 개수는 오른쪽에 따로 표시한다
    (등급별 개수 합계가 "모은 알"과 일치해야 한다 — 종류 진행도만 남겼더니 총 개수와 안 맞아 보인다는 지적)
17. docs: 공지글을 현재 완성 상태 기준 출시 공지로 전면 재작성
18. **후원 밴드 2줄 + 표시·소진 싱크**(사용자 지적) —
    (a) "쓰다듬기는 후원과 동시에 올라가는데 내역은 큐에 밀려 늦게 떠서 안 맞는다"
        → 표시 큐와 처리 큐를 통합. 지금 뜬 후원의 쓰다듬기만 그 시간 동안 나눠 소진하고
        진행 막대와 `3 / 10회` 카운터로 보여준다
    (b) "후원 내역이 아래에서 위로 움직여 눈에 안 들어온다"
        → 떠다니던 배너 오버레이를 없애고 밴드를 두 줄로: 윗줄 현재 내역, 아랫줄 누적 내역
19. **밸런스 확정 + 데모 후원 제거**(사용자 확정) — 시뮬레이션으로 "클릭 30분 + 후원 10~15만원"이면
    완성 확률 69~72%임을 확인하고 **현재 비율을 그대로 확정**했다(클릭이 주력, 후원은 가속기).
    ※ 후원만으로 채우려면 85만~120만원이 든다. "후원 10~15만원이면 완성"으로 안내하면 안 된다.
    소개가 끝나 데모 후원 버튼(🎬)과 `?demo=` 스위치를 전부 삭제. 테스트는 콘솔 `__gg.donate()`.
    ↺ 아이콘 버튼을 알아보기 쉽게 **[↺ 초기화]** 텍스트 버튼으로 교체
20. **가운데 칸 가독성 손질**(사용자 요청) — 타이머 아래 안내문을 지우고 "다음 알까지 3분마다 하나씩"
    한 줄로 병합, 그 공간으로 거위를 키우고(1000px 화면에서 323 → 400px) 24K 스트립 글씨를
    12 → 14.5px / 값은 17px 금색으로 키웠다. 거위 크기는 vh 기준이라 화면 높이에 맞춰 줄어든다
    (실측: 840px 여유 32px · 1000px 여유 115px, 어느 쪽도 잘리지 않음)

## 4-1. 확정된 운영 방식

- **스트리머는 평소 쓰는 크롬 창으로 접속하고, OBS는 그 창을 "창 캡처"로 잡는다.**
  OBS 브라우저 소스는 쓰지 않는다 — 내장 브라우저는 프로필이 달라 치지직 로그인을 다시 해야 하고,
  클릭하려면 "상호 작용" 창을 열어야 해서 클리커에 맞지 않는다.
- 소리는 OBS **응용 프로그램 오디오 캡처**(크롬)나 데스크톱 오디오로 잡는다.
- **탭을 가리거나 최소화하면 타이머가 멈추는 것은 의도된 동작이다**(사용자 확정, 2026-09-01).
  브라우저가 안 보이는 탭의 화면 갱신을 멈추기 때문인데, 고치지 않기로 했다.
  창을 완전히 닫았다가 다시 열면 그동안의 시간은 정상적으로 쳐준다(오프라인 누적).

## 5. 다음 할 일 (우선순위순)

1. ~~GitHub 저장소 생성 + Pages 배포~~ **완료** (2026-09-01, 저장소명 `golden-goose`)
2. ~~전용 앱·프록시 구축~~ **완료** — 워커 `golden-goose-proxy` 배포, `CHZZK_CLIENT_ID`/`CHZZK_CLIENT_SECRET` 등록됨
3. ~~치지직 로그인·후원 수신 확인~~ **완료** (2026-09-01, 테스트 후원 15건 수신 확인)
4. **실전 리허설** — 방송 켜고 시청자 후원 수신 확인 + OBS 브라우저 캡처(효과음은 창/데스크톱 오디오)
5. ⚠ **Client Secret 재발급 권장** — 초기 설정 때 Secret이 대화에 노출됐다.
   개발자센터에서 재발급 후 `cd proxy && printf '%s' '<새Secret>' | npx wrangler secret put CHZZK_CLIENT_SECRET`
   (개행이 붙으면 `INVALID_CLIENT`가 난다 — `printf '%s'` 필수)
6. (선택) 워커 `ALLOWED_ORIGINS`에 `https://goormigrm.github.io`를 넣어 프록시 잠그기 → `npx wrangler deploy`
7. (아이디어, 미구현) 알 판매·업그레이드·부화, 도감 완성 보상, 후원자별 기여 순위, OBS용 컴팩트 모드

## 6. 룰렛과의 관계

코드 출처만 같고 **운영은 완전히 분리**돼 있다. 한쪽을 건드려도 다른 쪽은 안 깨진다.

| | 먹방 룰렛 | GOLDEN GOOSE 24K |
|---|---|---|
| 치지직 앱 | `95337781-…` | `afd9f0b7-…` |
| 프록시 워커 | `mukbang-proxy` | `golden-goose-proxy` |
| 저장소 | mukbang-roulette | golden-goose |

`chzzk.js`는 룰렛 `src/chzzk.ts`의 바닐라 포팅이고, `proxy/worker.js`는 룰렛 워커에서 복사해 왔다.
**룰렛 쪽 연동 로직에 버그를 고치면 이쪽에도 같은 수정을 반영해야 한다**(자동 동기화 없음).

## 7. 개발 환경 메모 (이 PC 기준)

- **npm install 필요 없음.** `index.html`을 열면 돌아간다. OAuth를 테스트할 때만 서버가 필요하다(`npx serve .`).
- 이 세션에서는 mukbang-roulette의 vite를 빌려 `.claude/launch.json`에 임시 `goose` 설정을 만들어 8899 포트로 띄워 검증했다(작업 후 제거함).
- 콘솔 디버그: `__gg.state()` / `__gg.lay(n)` / `__gg.donate(원)`.
- git 커밋 메시지에 한글+특수문자를 쓸 때 PowerShell here-string이 깨질 수 있음 → 메시지 파일 + `git commit -F` 사용.
- 사용자 원칙: **요구하지 않은 임의 제약(개수 상한·글자수 컷 등) 금지.** 필요하면 먼저 물어볼 것.
